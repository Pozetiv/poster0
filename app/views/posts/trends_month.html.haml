%h2 Trends month #{(Time.now - 1.month).strftime("%B %Y")}
- if @posts.any? 
  - @posts.each do |post|
    = render partial: 'posts/post', locals: { post: @posts }
- else
  Sorry, but We haven't got a top of posts now