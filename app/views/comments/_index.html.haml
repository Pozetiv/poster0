-@comments.each do |comment|
  .media.comment
    =avatar_for_comments(comment.user)
    .media-body
      .mt-0
        =link_to comment.user.nick, "#"
        =time_ago_in_words(comment.created_at)
      =comment.body
